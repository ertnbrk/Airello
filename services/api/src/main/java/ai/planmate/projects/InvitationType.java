package ai.planmate.projects;

/** Type of invitation for project access. */
public enum InvitationType {
    EMAIL, // Invitation sent to specific email address
    LINK // Anonymous invitation via shareable link
}
